#!/usr/bin/env python3

import sys
import os

# Add the repo root to sys.path so xlade package is found
# without requiring pip install
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from xlade.cli.main import main

if __name__ == "__main__":
    main()