# xLaDe Runtime State

## Global State
Stored in `~/.xlade/`:

- config.toml — user configuration
- mode — active ecosystem mode
- runs/ — experiment execution records

## Project State
Stored in `.xlade/` inside a project:

- experiments.lock
- metrics.json
- last-run
